# nginx.conf for tasky-ui: proxy /api to the backend service "api" in docker-compose
# Also serve SPA index.html for other routes (history API fallback)

# Upstream backend uses the Docker Compose service name 'api'
upstream backend {
  server api:8080;
}

server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Proxy API requests to the backend service (via upstream 'backend')
  # We proxy the full request URI to the upstream (no path rewriting here).
  location /api/ {
    proxy_pass http://backend;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Timeouts and connection settings
    proxy_connect_timeout 5s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    # Use HTTP/1.1 for keepalive to upstream
    proxy_http_version 1.1;
    proxy_set_header Connection "";

    # Don't buffer large responses (optional)
    proxy_buffering off;
  }

  # Serve static files, fallback to index.html for SPA routing
  location / {
    try_files $uri $uri/ /index.html;
  }
}
